/**
 * Copyright （c）<2021> <Max C. Foo>
 * MIT LICENSE
 */

const School = require('./api/school');
const GetInfo = require('./api/get_info');

module.exports = {
    School: School,
    GetInfo: GetInfo,
}